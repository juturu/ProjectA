<Page
    x:Class="FlickrPhotos.Views.FlickrAlbums"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FlickrPhotos.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:triggers="using:WinRT.Triggers"
    xmlns:controls="using:Callisto.Controls"
    mc:Ignorable="d"
    DataContext="{Binding Source={StaticResource ViewModelLocator}, Path=FlickrAlbumsViewModel}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <GridView ItemsSource="{Binding AlbumsCollection}" IsItemClickEnabled="True">
            <triggers:Interactions.Triggers>
                <triggers:EventTrigger EventName="ItemClick">
                    <triggers:InvokeCommandAction Command="{Binding Path=AlbumClickedCommand}" PassEventArgsToCommand="True"></triggers:InvokeCommandAction>
                </triggers:EventTrigger>
            </triggers:Interactions.Triggers>
            <GridView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                    <TextBlock x:Name="AlbumNameTextBlock" Text="{Binding Name}" Visibility="Collapsed"></TextBlock>
                    <controls:LiveTile ItemsSource="{Binding AlbumPhotos}" Direction="Up" MinHeight="300" MinWidth="300">
                        <controls:LiveTile.ItemTemplate>
                            <DataTemplate>
                                <Grid x:Name="ImageGrid">
                                    <Image Source="{Binding Path=PhotoUri}" Stretch="UniformToFill"></Image>
                                    <StackPanel Orientation="Vertical" Background="#CC000000" Height="90" VerticalAlignment="Bottom">
                                        <TextBlock Text="{Binding ElementName=AlbumNameTextBlock, Path=Text}" Margin="10,3,0,0" Width="242" Height="62" 
                                   TextTrimming="WordEllipsis" TextWrapping="Wrap" HorizontalAlignment="Left"/>
                                    </StackPanel>
                                    
                                </Grid>
                            </DataTemplate>
                        </controls:LiveTile.ItemTemplate>
                    </controls:LiveTile>
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapGrid Orientation="Vertical"></WrapGrid>
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>
            
        </GridView>
    </Grid>
    <Page.BottomAppBar>
        <CommandBar IsSticky="True" IsOpen="True">
            <CommandBar.SecondaryCommands>
                <AppBarButton Icon="preview" Command="{Binding AlbumsViewCommand}"></AppBarButton>
                <AppBarButton Icon="refresh" Command="{Binding RefreshCommand}"></AppBarButton>
            </CommandBar.SecondaryCommands>
        </CommandBar>
    </Page.BottomAppBar>
</Page>
